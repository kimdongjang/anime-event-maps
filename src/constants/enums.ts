export enum MainCategory {
  MAIN = 1,
  FAVORITE = 2,
  CALENDER = 3,
}

export enum ISort {
  /** 최근 순 */
  DATE_DOWN = 2,
}

export enum FilterType {
  /** 행사별 */
  EVENT = 1,
  /** 행사장별 */
  LOCATION = 2,
  /**지역별 */
  ADDRESS = 3,
}
